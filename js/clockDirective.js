angular.module("myApp").directive("clock", function($scope, $interval){


  return {
    restrict: "AE",
    templateUrl: "test.hml",
    link: function(scope, element, attr){

          }
    }
  }


})
